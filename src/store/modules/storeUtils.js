import Vue from 'vue'
import i18n from "@/lang/lang";

//import BootstrapVue from 'bootstrap-vue'
//Vue.use(BootstrapVue)

export default {
    state :
    {
    },
    getters :
    {
    },
    actions :
    {
        /*confirm(context, opz)
        {
            this._vm.$swal({
                title: i18n.t('confirmMessage.title'),
                text: i18n.t('confirmMessage.text'),
                type: "warning",
                showCancelButton: true,
                confirmButtonColor: "#4caf50",
                cancelButtonColor: "#f44336",
                confirmButtonText: i18n.t('confirmMessage.yes'),
            }).then(result => {
                if (result.value) {
                    opz.callback();
                }
            });
        },*/
        /*toast(context, opz)
        {
            this._vm.toasted.toast(i18n.t(opz.msg), {
                variant: opz.variant,
                toaster: 'b-toaster-bottom-right',
                solid: true,
                duration : 1000
            });
        },*/
    },
    mutations :
    {
    }
}
